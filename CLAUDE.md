@AGENTS.md

# Claude Code

- Treat `AGENTS.md` as the single source of truth for shared coding-agent instructions.
- Start Claude Code from the repository root so the shared instructions and Gradle commands resolve correctly.
